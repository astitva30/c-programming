#include <stdio.h>

int main()
{
    int length , bredth ;
    length=7;
    bredth=5;
    printf("Perimeter of rectangle = %d inches\n" , 2*(length+bredth));
    printf("Area of rectancle is = %d square inches\n", length*bredth);
    return(0);

}